name: "Autosend"

on:
  push:
    branches: master
  schedule:
    - cron: 21 19 */1 * * # 每10天8点30自动执行，北京时间减8个钟头
  workflow_dispatch:
    inputs: {}

jobs:
  autosend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: pushBotMsg
        uses: joelwmale/webhook-action@master
        with:
          url: "https://api.groupme.com/v3/bots/post"
          body: '{"text" : "Have a Nice Day!hahaha", "bot_id" : "${{ secrets.GM_BOT_ID }}" }'
